(function (GAME) {
	// ---------------------------
	// Each Actor should have following methods:
	//     - start ()				starts the actor's impact on the GAME.Model
	//     - pause ()				pauses activity; actor should not impact the GAME.Model
	//     - isActive ()			returns boolean value
	//     - winsWhen ()			returns `true` when the actor wins according the scenario
	// ---------------------------
	GAME.Actors = {};
})(GAME);
